export interface itemSpaseX {
  flight_number: number;
  links: {
    mission_patch: string;
    mission_patch_small: string;
  };
  details: string;
  rocket: {
    rocket_name: string;
  };
  mission_name: string;
}
